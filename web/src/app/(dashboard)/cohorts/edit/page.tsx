export default function EditCohort() {
  return (
    <>
      <p>
        Edit an existing cohort page that populate with the existing data,
        allows for edit and submission to update on the backend
      </p>
    </>
  );
}
